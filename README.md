# 🧩 Assignment

A simple React + TypeScript project built with **Vite**, styled using **Tailwind CSS**, and featuring **Lucide React** icons.

---

## 🚀 Getting Started

### 1️⃣ Clone the repository

```bash
git clone <your-repo-url>
cd assignment
This will install:

React + TypeScript (via Vite)

Tailwind CSS (for styling)

Lucide React (for icons)

Any other dependencies defined in package.json

🛠️ Development

To start the local development server:

npm run dev


Then open http://localhost:5173
 in your browser.

🎨 Styling

This project uses Tailwind CSS.
To customize styles, edit:

tailwind.config.js – for theme settings like colors, fonts, etc.

src/index.css (or src/main.css) – for global styles and font imports.

Example Roboto setup:

@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap');
@import "tailwindcss";

:root {
  font-family: 'Roboto', sans-serif;


